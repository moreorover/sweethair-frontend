mutation RemoveCustomerToAppointment(
  $appointmentId: Float!
  $customerId: Float!
) {
  removeCustomerToAppointment(
    appointmentId: $appointmentId
    customerId: $customerId
  ) {
    id
    customers {
      id
    }
  }
}
