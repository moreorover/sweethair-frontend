mutation CreateTransaction($transaction: TransactionCreate!) {
  createTransaction(transaction: $transaction) {
    id
    total
    isPaid
    scheduledAt
    type
    customerId
    appointmentId
    invoiceId
  }
}
